# -*- mode: yaml -*-
#
# Caelus Python Conda environment configuration file
#
# Usage:
#   conda env create -f <path_to_this_file>

name: caelus-env

dependencies:
  - python=2.7
  - pip
  - six
  - numpy
  - scipy
  - pandas
  - matplotlib
  - ipython
  - pyyaml
